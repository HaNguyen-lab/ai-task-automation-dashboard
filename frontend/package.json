
{
  "name": "ai-task-automation-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "axios": "^1.6.7",
    "react-chartjs-2": "^5.2.0",
    <body>
    <div id="root"></div>
    /src/main.jsx</script>
  function suggestTasks(tasks) {
  const res = await axios.post(`${API_BASE}/suggest`, { tasks })
  return res.data
}

export async function automateTasks(tasks) {
  const res = await axios.post(`${API_BASE}/auto`, { tasks })
  return res.data
}
